# Note: using 'qtconfig', you can set qt to use the active gtk theme (may have
# to adjust the 'Window' and 'Base' colors).

gtk-font-name="Bitstream Vera Sans 8"
include "/usr/share/themes/Greybird/gtk-2.0/gtkrc"

# vim:nowrap
